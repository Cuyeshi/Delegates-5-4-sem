<Window x:Class="Lab_5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timer App" Height="200" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="10" Text="Введите таймаут (секунды):"/>
        <TextBox x:Name="TimeoutTextBox" Grid.Row="1" Margin="10" HorizontalAlignment="Stretch"/>
        <Button x:Name="StartTimerButton" Grid.Row="2" Margin="10" Content="Start Timer" Click="StartTimerButton_Click"/>
    </Grid>
</Window>
